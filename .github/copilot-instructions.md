# KevinOS for Copilot

Follow `AGENTS.md`. The app stays a single dependency-free ES5-style `index.html`; use current domain contracts under `docs/` and run `node tools/doctor.js` plus `sh test/run.sh`.
