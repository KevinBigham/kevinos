# macOS
.DS_Store

# Cloudflare Worker local cache / state (contains account id — never commit)
.wrangler/
relay/.wrangler/

# node
node_modules/
.claude/
.playwright-cli/
.playwright-mcp/

# Local credentials and environment files — never commit
.env
.env.*
!.env.example
.dev.vars
.dev.vars.*
!.dev.vars.example
relay/.dev.vars
relay/.dev.vars.*
!relay/.dev.vars.example
secrets.local
*.secrets.local
output/credentials/
output/provider-live/
