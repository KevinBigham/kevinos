# KevinOS work packet

## Outcome

One user-visible result and why it improves attention, trust, speed, or recovery.

Packet version: `1`
Packet fingerprint: generated from the saved Studio mission

## Current evidence

Exact behavior, source symbols, failing test, or browser reproduction.

## Scope

- Allowed files:
- Forbidden files:
- Data/schema classification:
- Security/privacy impact:

## Implementation contract

Smallest compatible behavior, existing patterns to reuse, and explicit non-goals.

## Acceptance

- `[acceptance-id]` Expected observable result:
- `[acceptance-id]` Expected observable result:

Each item ends as `pending`, `pass`, `fail`, or `waived`; a waiver requires a reason and does not become a pass.

## Verification contract

- Focused automated checks:
- Full gate:
- Manual surfaces/viewports:
- Evidence required:
- Which results can be verified locally:
- Which results can only be collaborator-reported:

Returned work must quote the packet fingerprint. Reported success is not local proof. Preserve failed attempts instead of overwriting them.

## Authority gates

State whether schema, deploy, push, secrets, outward actions, or destructive changes are excluded or require approval.
